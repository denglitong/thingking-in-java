/**
 * 枚举类型的实例是常量，按惯例命名都要大写
 * 尽管enum看起来像是一种新的数据类型，但是这个关键字只是在为enum生成对应的类时，
 * 产生了某种编译器行为
 * @autor denglitong
 * @date 2019/7/23
 */
public enum Spiciness {
    NOT,
    MILD,
    MEDIUM,
    HOT,
    FLAMING,
}
